# Obsidian 설정 배포 체계

> 상태: **자동 배포는 안 하기로 함** (2026-07-16 결정)
> 핵심 문제는 이미 해결됐다. 나머지는 배꼽이 배보다 커서 접었다.
> 이 문서는 **조사 기록**으로 남긴다 — 다시 꺼낼 때 처음부터 파지 않기 위해.
> 관련: [[문서-링크-규칙]], [[Obsidian-단축키]]

## 한 줄 결론

**심각한 문제의 답은 gitignore 한 줄이었다.** 배포 체계가 필요한 문제인 줄 알았는데 아니었다.

## 원래 문제 두 개

레포마다 `docs/`가 옵시디언 볼트고, `docs/.obsidian`이 git에 추적되고 있었다.

1. **배포의 문제** — 설정을 개선하면 레포마다 손으로 복사하고 커밋해야 한다. (가성비 좋은 쪽)
2. **의도치 않은 설정 동기화** — 누가 옵시디언을 만지면 그 변경이 `git add`에 딸려 올라가고, pull한 사람의 설정이 같이 바뀐다. **아무도 의도한 적 없는데 벌어지고, 겪기 전엔 아무도 모른다.** (심각한 쪽)

**근본 원인:** `docs/.obsidian` 한 폴더가 두 역할을 겸직했다 — 옵시디언이 실시간으로 쓰는 작업 폴더이자, git이 추적해 남에게 나르는 공유 폴더. 통로가 하나라 방향을 고를 수 없었다. 내가 밀면 남에게 가고(원함), 남이 밀어도 나에게 온다(안 원함).

---

## ✅ 완료: `.obsidian` 추적 해제 (2026-07-16)

**2번(심각한 쪽)이 이걸로 끝났다.** 추적을 안 하면 역류할 통로가 없다.

세 레포 루트 `.gitignore`를 이렇게 바꿨다:

```gitignore
# ===== Obsidian 볼트 =====
# .obsidian 은 개인 것이다. 어느 폴더를 볼트로 삼든(docs/ 뿐 아니라) 공유하지 않는다.
.obsidian/
```

`docs/.obsidian/`이 아니라 `.obsidian/`인 이유: **옵시디언 설정은 어느 폴더에 있든 개인 것**이다. 나중에 다른 폴더를 볼트로 삼아도 자동으로 걸린다. 옛 규칙(`docs/.obsidian/workspace.json` 두 줄)은 지웠다.

| 레포 | 커밋 |
|---|---|
| ESG_ChatBot | `063e97a` |
| AgentShe_ChatBot | `04355b5` |
| Agentworks_Infra | `0c83c26` |

`git rm -r --cached`로 **파일은 디스크에 두고 git 기억에서만** 지웠다. 확인: 추적 0개, 워킹트리 깨끗, 디스크 파일 76개 그대로, 볼트 표시명 3개 정상.

> ⏰ **타이밍이 결정적이었다.** git에게 "이 파일들 잊어"는 커밋에 **"삭제"로 기록**된다. 즉 **이 전환 커밋을 pull하는 사람의 `.obsidian`은 진짜로 지워진다** — 테마·플러그인·핫키 전부. 그리고 `.gitignore` 때문에 git이 되살려주지도 못한다. `git status`는 깨끗하고 본인은 옵시디언을 열기 전까지 모른다. (샌드박스 실증)
>
> **지금은 요한 혼자라 다칠 사람이 없었다.** 사람이 늘고 나서 했다면 공지하고 백업받고 난리를 쳐야 했다. 미룰수록 비싸지는 일이었고, 오늘 해서 공짜로 끝났다.

## ✅ 완료: 설정 원본 레포 (2026-07-16)

**`github.com/yohan9230/obsidian-setup`** — 요한 개인 계정, **공개**. 조직 레포가 아니다(회사 것이 아니라 요한의 편집 환경이므로). 필요한 사람에게 링크를 주면 된다 — private 이면 링크가 안 통해서 공개로 갔고, 옵시디언 설정이라 숨길 게 없다.

**자동화 없이도 남는 이득이 이거였고, 다 얻었다:**
- **"어느 게 진짜 원본이냐"가 사라졌다.** 그 레포의 `obsidian/` 이 정본이다
- **변경 이력이 남는다.** 원드라이브 볼트는 git이 아니라 누가 뭘 바꿔도 알 길이 없었다
- **자작 플러그인 2개의 집이 생겼다.** 그전엔 볼트 안 `main.js` 로만 존재해 이력도 백업도 없었다 — 볼트 하나 날아가면 그냥 사라지는 상태였다

출발점은 원드라이브 볼트(가장 최신)였다. **공개 전 전수 점검 결과 토큰·회사경로 0건**이고, `workspace.json` 만이 진짜 위험이었다(최근 연 파일 목록에 회사 문서 제목이 그대로).

**`vault.ps1` 도 만들었다** — "선택·급하지 않음"으로 미뤄뒀던 그것이다. `check` / `sync` / `capture` 세 명령뿐이고 **제외 목록이 코드 안에 있다.** 요한 PC 안에서만 돌아 남을 다치게 할 경로가 없다. `vault-sync` 스킬이 감싸고 있어 "볼트 최신화" 같은 말에 걸린다.

> 손으로 복사하면 안 되는 이유가 [[Obsidian-설정-배포-체계#배포 제외 목록|아래]]에 있다. **메모리에 목록이 적혀 있어도 손으로 하면 틀린다** — 그래서 코드에 넣었다.

## ❌ 안 하기로 한 것: 자동 배포

원본 → 조직 레포들로 GitHub Actions가 자동 배포하는 체계. `.vault-template` 폴더, 표식 파일, `init`/`deploy`/`capture`/`setup` 스크립트, 토큰 관리 전부.

### 왜

**만들 것** — Actions, 토큰(만료 관리), `init`, `deploy`, `capture`, `setup`, 표식, 제외목록 공유 함수, 안전장치 4개. 그리고 **요한 혼자 평생 유지보수.**

**얻을 것** — 복사 3번이 명령 1번으로. **하루 몇 분.** 게다가 옵시디언 설정은 언젠가 수렴한다. 지금 하루 3개씩 고치는 건 세팅 중이라 그런 거지 영원하지 않다.

**리스크** — 잘못 건드리면 **레포를 공유하는 모든 사람의 설정·플러그인·스니펫이 한 번에 날아간다.** `.obsidian`은 gitignore라 **git이 되살려주지 못한다.** 백업이 없으면 끝이고, 버그를 발견했을 땐 이미 여러 명 것이 사라진 뒤다.

**배꼽이 배보다 크다.** 그리고 결정적으로 — **애초 취지("clone한 사람이 세팅 없이 바로 쓰게")를 위한 사람이 아직 존재하지 않는다.** 없는 사람을 위해 만들고 평생 유지보수하는 건 손해다.

### 다시 꺼낼 조건

**실제로 사람이 늘어서, "clone하면 바로 되게"가 없는 게 진짜로 아쉬워질 때.** 그때 이 문서를 열면 조사도 함정도 다 있다.

그때 **반드시 기억할 것**: 위 ⏰ 참조. 전환은 사람이 늘기 전에 해야 공짜다. 이미 늦었다면 전환 커밋보다 **먼저** "pull 전에 `docs/.obsidian`을 복사해두세요"를 공지해야 한다.

---

# 조사 기록

여기부터는 **다시 꺼낼 때 처음부터 파지 않기 위한 기록**이다.

## 배포 제외 목록

**여기에 적지 않는다.** 정본은 이 레포 루트 `vault.ps1` 의 `$PerVaultFiles` 하나뿐이다.

초안에서는 이 문서에 목록을 표로 적어뒀는데 **하루 만에 낡았다**(BRAT를 지워서 개수가 바뀌었다). 같은 목록이 메모리·이 문서·`.gitignore`·스크립트 네 곳에 살고 있었고, 그중 둘이 이미 틀려 있었다. 요한 규칙 그대로다 — **"같은 경로를 여러 문서에 적지 않는다. 한 곳에만 적고 나머지는 그곳을 가리킨다."**

전수 조사 결과 중 **목록이 아닌 것**만 여기 남긴다.

- ⚠️ **`plugins/dragger/data.json`은 제외 대상이 아니다.** 항상 dirty해 보여 개인 파일로 의심했으나 **네 볼트가 바이트 단위로 동일**하다.
- 나머지(core 설정 9종, 플러그인 data.json 8종, 테마, 스니펫, 핫키)는 **전부 공유 안전**함이 확인됐다.
- `obsidian42-brat/data.json` 은 GitHub 토큰 칸 때문에 제외 대상이었으나 **BRAT 플러그인 자체를 지워서(2026-07-16) 문제가 사라졌다.** list-cycler 는 BRAT 없이도 잘 돈다 — BRAT는 설치·자동업데이트만 하는 도구고, 그 플러그인은 21개월째 릴리스가 없었다.

> 🚨 **왜 손으로 복사하면 안 되는지 — 2026-07-16 실제 사고.** 요한이 원드라이브 볼트를 세 레포에 통째로 복사했다가 **볼트 표시명 3개가 전부 "개인 원드라이브"로 덮였다.** 커밋 직전 `git status`를 보고 `git restore`로 복구했다. **그때도 제외 목록은 메모리에 적혀 있었다.** 적혀 있어도 손으로 하면 틀린다 — 그래서 목록을 코드 안에 넣고 스크립트로 돌리는 것이다.

## 원본에서 고칠 것

원본 레포를 만들 때 마스터 볼트에서 정리한다.

- **`templates.json`의 경로를 지운다** — `Obsidian-사용/_템플릿`을 가리키는데 네 볼트 어디에도 없는 폴더다
- **유령 스니펫 `file-explorer-tweaks` 제거** — 네 볼트 모두 활성화돼 있으나 css 파일이 어디에도 없다
- **(선택) 마스터 볼트에 `README.md` 추가** — `first-run-layout`은 `목록.md`를 왼쪽에, `README.md`를 오른쪽에 연다. 마스터 볼트엔 README가 없어서 **이름순 첫 문서로 폴백**해 `AgentShe-백엔드스택-결정-인계.md`가 열린다. 실패는 아니고(코드에 폴백·알림이 다 있다) `workspace.json`이 없는 첫 실행 때만 겪는다. 오른쪽에 뜰 문서를 정하고 싶으면 README를 만들면 된다.

> 초안에 "마스터 볼트엔 README가 없어서 first-run-layout이 이미 실패 중"이라고 적었으나 **틀렸다.** 코드를 읽어보니 폴백(`others[0]`)이 있고, 둘 다 없으면 알림까지 띄운다. 목차만 있고 첫 문서가 없으면 창을 쪼개지도 않는다.
- ~~`baseFontSize` 19로 통일~~ · ~~`attachmentFolderPath` = `assets`~~ — **완료.** 2026-07-16 복사로 네 볼트가 동일해졌다

## 마스터 볼트에만 있는 것 (레포 3곳에 배포 완료)

2026-07-16에 손으로 복사·푸시했다 (`cfe47eb`/`d4f6817`/`680f717`).

- 자작 플러그인 **`vault-switcher-nicknames`** — 볼트 전환 팝업에 폴더명 대신 닉네임 표시. 각 볼트의 `vault-nickname/data-shared.json`을 읽는다
- 자작 플러그인 **`first-run-layout`** — 볼트 첫 실행 시 화면 배치
- 스니펫 `callout-info-readable.css`
- 핫키 **Alt+V** (볼트 전환) — `vault-switcher-nicknames`를 부르는 유일한 키라 **플러그인과 세트로** 가야 한다

## 기각한 대안 (재조사 방지)

| 대안 | 왜 기각 |
|---|---|
| **Override config folder** (옵시디언 네이티브) | 볼트 **경로**를 열쇠로 기억한다. 폴더를 옮기면 **말없이 `.obsidian`으로 되돌아가 다시 새기 시작한다.** 스크립트로 걸 수 없고(GUI 수작업+재시작), 바꿔도 기존 설정이 안 따라온다. **고장나도 조용한 게 치명적** |
| dev.to의 git+override 방식 | 추적 폴더가 곧 옵시디언이 쓰는 폴더라 2번 문제가 그대로. 글 자체가 충돌나면 `git reset --hard` 하라고 안내한다 — 문제를 못 풀고 같이 사는 것 |
| 폴더 통째 심볼릭 링크 | 옵시디언 커뮤니티가 명시적으로 경고. 캐시·로컬 경로가 볼트 경계를 넘어 샌다 |
| git submodule | clone 옵션 하나 빠뜨리면 빈 폴더. 갱신하려면 어차피 각 레포에 커밋이라 비용은 같은데, 남이 submodule을 알아야 함 |
| 대상 레포 목록 파일 | 레포가 늘면 추가를 까먹고 그 레포만 조용히 뒤처진다 |

**볼트 간 설정 공유는 옵시디언에 기능이 없다** (미해결 기능 요청 상태). 커뮤니티가 실제로 쓰는 방법도 "마스터 볼트 + 복사 스크립트"뿐이다.

## 다시 하게 되면 밟을 지뢰

적대적 검토에서 나온 것들. 전부 샌드박스 실증 또는 공식 문서 확인.

- **전환 pull이 협업자의 `.obsidian`을 통째로 지운다** — 위 ⏰ 참조. **제일 크다.**
- **gitignore 패턴은 슬래시가 없으면 모든 깊이에서 매치된다** — `Obsidian-단축키.md`라고 쓰면 배포용 원본(`.vault-template/notes/Obsidian-단축키.md`)까지 **경고 없이** 삼킨다. `/Obsidian-단축키.md`로 앵커할 것
- **표식 파일을 배포 폴더 안에 두면 첫 배포에 스스로 지워진다** — 1회차는 성공하고 2회차부터 조용히 명단에서 빠진다. 폴더 **밖**에 둘 것
- **`docs/CLAUDE.md`는 "그 폴더 파일을 *읽을* 때"만 로드된다** — 새 문서를 *쓸* 때는 로드 안 된다. "규칙이 반드시 읽힌다"는 전제가 반만 맞다. 루트 `CLAUDE.md`의 `@import`가 후보이나 미검증
- **배포 폴더가 옵시디언에 안 잡히는 건 이름이 `.obsidian`이 아니라서가 아니라 점(`.`)으로 시작해서다** — 점을 떼면 같은 이름 문서가 둘 생기면서 즉시 터진다
- **`.gitattributes`는 소급 적용되지 않는다** — 이미 커밋된 CRLF는 줄만 추가해선 안 고쳐지고 **오히려 없던 dirty가 생긴다.** `git add --renormalize`가 필요. 그리고 슬래시 든 패턴은 그 파일이 있는 폴더에 앵커되므로 **루트에 두면 `docs/` 아래를 못 잡는다**
- **"항상 dirty"는 재현되지 않았다** — 초안이 0단계로 올렸던 근거인데 실측상 세 레포 다 깨끗했다. 진단이 틀렸던 것
- **기본 `GITHUB_TOKEN`은 다른 레포에 못 쓴다** — PAT나 GitHub App이 필요. PAT는 **만료되면 배포가 조용히 멈춘다.** 그리고 진짜 방벽은 토큰이 아니라 "표식 없으면 안 건드린다"는 코드다
- **설치 로직이 두 벌 된다** — 남용(덮어쓰기 거부)과 요한용(덮어씀). 제외 목록이 두 곳에 살면 언젠가 어긋나 표시명 사고가 재발한다. **위험한 쪽을 요한 혼자 써서 아무도 검증하지 않는다**
- **전환 후엔 엉뚱한 볼트에서 개선하면 조용히 증발한다** — 지금은 `git status`가 그물인데, `.obsidian`이 gitignore면 흔적이 안 남는다

## 🚫 절대 금지

`C:\Users\yohan\OneDrive - 에이전웍스\AgentWorks - General\` 이하는 **절대 건드리지 않는다.** 회사 공유 폴더라 바꾸면 남에게 즉시 퍼진다. 거기 있는 반쪽 볼트(`AgentSHE\.obsidian`, 5파일)도 대상이 아니다.
