# obsidian-setup — 폴더 인덱스

**요한의 Obsidian 편집 환경 원본.** 설정·플러그인·테마·스니펫·단축키가 통째로 있다. 요한 **개인** 저장소이고 **공개**다 — 필요한 사람에게 링크를 준다.

> 📄 이 저장소는 루트가 곧 볼트다(`docs/` 를 따로 두지 않는다 — 문서가 몇 개뿐이라). 목차 = `목록.md`.
> ⚠️ **배포물 폴더 이름은 `obsidian/` 이지 `.obsidian/` 이 아니다.** 점을 붙이면 `.gitignore` 가 삼키고, 이 저장소 자체가 볼트로 오인된다.

| 항목 | 설명 |
|------|------|
| `.gitattributes` | Git 속성 — 텍스트 EOL을 LF로 고정. Obsidian이 LF로 쓰므로 맞추지 않으면 내용이 같은데도 가짜 변경이 낀다. |
| `.gitignore` | Git 제외(개인 `.obsidian`·내 PC 경로·볼트마다 달라야 하는 파일). |
| `obsidian/` | **설정 원본.** 볼트의 `.obsidian` 으로 복사할 내용 전부. 여기가 정본이다. |
| `Obsidian-단축키.md` | 이 설정의 단축키 안내. 커스텀이 섞여 있어 설정과 세트로 간다. |
| `README.md` | 설치법·들어있는 것·플러그인 출처. 받는 사람이 처음 읽는 곳. |
| `vault.local.json` | 내 PC의 볼트 경로. gitignore — 공개 저장소라 경로에 회사 정보가 드러나면 안 된다. |
| `vault.ps1` | 볼트 동기화 스크립트(`check`/`sync`/`capture`). **볼트마다 달라야 하는 파일 목록의 정본**(`$PerVaultFiles`) — 다른 곳에 옮겨 적지 말 것. |
| `목록.md` | 이 저장소를 볼트로 열었을 때의 목차. |
| `문서-링크-규칙.md` | 이 환경이 전제하는 문서 구조·링크 규칙. |

> ※ `.git`(숨김)은 인덱스에서 제외.
> 🚫 **볼트를 손으로 복사하지 마라** — 볼트마다 달라야 하는 파일이 날아간다. `vault.ps1` 을 쓴다. (`vault-sync` 스킬이 감싸고 있다.)
